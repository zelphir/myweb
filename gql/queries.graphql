query GetAllTags {
  allTags {
    id
    name
  }
}

query GetTodayLanguages($from: DateTime!, $to: DateTime!) {
  allLanguages(filter: { createdAt_gte: $from, createdAt_lte: $to }) {
    id
    entries
  }
}
