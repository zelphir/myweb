#import "./EntriesFragment.graphql"

query GetAllTags {
  allTags {
    id
    name
  }
}

query GetDailyStats {
  allDailyStats {
    id
    ...Entries
  }
}

query GetAllNullLanguages {
  allLanguages(filter: { day: null }) {
    id
  }
}
