query GetAllTags {
  allTags {
    id
    name
  }
}

query GetTodayLanguages($from: DateTime!, $to: DateTime!) {
  allLanguages(filter: { date_gte: $from, date_lte: $to }) {
    id
    date
    entries
  }
}
